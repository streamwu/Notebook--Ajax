<h1>Editing Note #<%= @note.id %></h1>

<%= form_tag(note_url(@note), method: 'patch') do %>
    <%= label_tag :content %><br />
    <%= text_field_tag :content, @note.content %>
    <%= submit_tag "Update Note" %>
<% end %>
